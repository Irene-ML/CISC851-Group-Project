"""_description_
"""
import nn

def uncorr_onestep(agent, sigma):
    """
    Apply uncorrelated update on genes of agent
    Args:
        agent (nn): An agent
        sigma (float): step size
    """
    
    
def uncorr_nstep(agent, sigma):
    """
    Apply uncorrelated update on genes of agent
    Args:
        agent (nn): An agent
        sigma (np.ndarray): step size for each gene
    """
